\documentclass[../main.tex]{subfiles}
\begin{document}

\section{Introduction}
\label{sec:introduction}

% Scaffold content --- replace with the real introduction. It exercises the
% whole toolchain (theorem environments, cleveref, biblatex, tikz-cd) so a
% clean build here means the setup is healthy.

This paper studies the cube-chain category $\Ch(K)$ of a precubical set $K$
and its executions, following Paliga and Ziemia\'nski~\cite{PZ} and
Ziemia\'nski~\cite{Z}.

\begin{theorem}[placeholder]
\label{thm:placeholder}
There is nothing to prove here yet.
\end{theorem}

\Cref{thm:placeholder} is a placeholder. A sample commuting square, to confirm
\texttt{tikz-cd} renders:
\[
\begin{tikzcd}
  \cube{m} \arrow[r, "f"] \arrow[d, "\pi"'] & K \\
  \cube{n} \arrow[r, "g"'] & \Ch(K) \arrow[u, hook]
\end{tikzcd}
\]

\end{document}
