\documentclass[11pt]{article}
\usepackage{preamble}
\usepackage{subfiles} % load last: enables per-section standalone compilation

% Previous working title: "Braid Arrangements for Concurrent Programs".
\title{Complexified Cube Chains and a Braid Invariant of Precubical Sets}
\author{% TODO: author name(s) and affiliation.  Left blank deliberately ---
        % do not circulate until this is filled in.
  }
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Ziemiański's cube chain category $\Ch(K)$ is a combinatorial model for the space of directed paths
on a precubical set $K$. Paliga and Ziemiański built a braid invariant for precubical sets 
by observing a relation between $\pi_1|\Ch(Z)|$ and braids, where $Z$ is the termainal precubical set.
However, that invariant trivializes for Euclidean $K$ because $|\Ch(\cube n)|$ is
contractible \cite[Prop.~6.1]{PZ}.
Following an analogy from the hyperplane arrangement literature, we build a ``complexified''
version of $\Ch$, which we call $\Chs$. It does not collapse: in fact
$\Chs(\cube n) \cong \Sal(n)\op$, the Salvetti poset of the braid arrangement. Salvetti's
theorem tells us $\pi_1|\Chs(\cube n)|$ is isomorphic to the pure braid group $P_n$.
The construction of $\Chs$ works by attaching one dimensional refinements to each 
cube chain in $K$. Futhermore, we associate permutations with each morphism, which give us a 
functor $\Conc : \Chs(K) \to \BraidPos$ into the positive braid monoid.
It is natural in $K$, and non-trivial as long as $K$ contains a 2-cell.
The construction and the comparison with $\Sal(n)$ are formalized in Lean \cite{Cubical}.
\end{abstract}

\tableofcontents
\bigskip

\section{Introduction}\label{sec:intro}
\subsection{Background}

Ziemiański's cube chain category $\Ch(K)$ is a combinatorial model for the executions of a
bipointed precubical set $K$ \cite[Thm.~7.6]{Z2}. Paliga and Ziemiański established a homotopy 
equivalence between $|\Ch(Z)|$ and configuration spaces of points in the plane, 
where $Z$ is the terminal precubical set.
It extends, by naturality, to an invariant of any precubical set $K$.
However, it trivializes on any euclidean complex, see {\cite[Prop.~6.1]{PZ}}. 
The proof is one line: the invariant factors through $|\Ch(\cube n)|$, which is contractible.

The key observation came through an attempt to formalize morse flows for precubical sets.
Consider the space of ``execution times'' as a vector in $\R^n$, where each coordinate is an event,
and the values in the vector represent when that event occured.
The association of vectors with cube chains is precisely the faces of the braid arrangement.
The trivialization phenomenon also occurs for hyperplane arrangements.
However, that literature has a solution: consider analogous hyperplane arrangements in $\C$ instead of $\R$. 
This results in the Salvetti poset \cite{Salvetti, Paris, BjornerZiegler}, whose elements are 
pairs of faces and minimal faces refining them.
Despite discovering neither a suitably generic morse framework, nor a notion of ``complexified precubical sets'',
the construction of the salvetti complex generalizes well.

The minimal faces of an arrangement are called chambers or topes.
In the precubical setting, these correspond to one dimensional cube chains, we call \emph{runs}. 
So we push this analogy through, and build $\Chs$ as a set of pairs, whose first component is an element of $\Ch$, 
and second component is a run refining it. The construction $\Chs(K)$ works for any precubical set, and is indeed natural in $K$. 
Futhermore we find the analogy with hyperplane arrangements is precise for cubes:
\begin{theorem}[\Cref{thm:sal}]\label{thm:A}
  For every $n \ge 0$ there is an isomorphism of posets $\Chs(\cube n) \cong \Sal(n)\op$, where
  $\Sal(n)$ is the Salvetti poset of the braid arrangement $A_{n-1}$. Consequently
  $|\Chs(\cube n)| \simeq \Conf(n, \C)$ and $\pi_1|\Chs(\cube n)| \cong P_n$, the pure braid group
  on $n$ strands. 
\end{theorem}

So indeed this circumvents the issue with euclidean complexes from {\cite[Prop.~6.1]{PZ}}.
Futhermore, the braid data can also be extracted directly from morphisms of $\Chs(K)$. 
No need to pass through Salvetti's theorem or nerves at all. 
The morphisms contain permutation data that allows us to build braid words in the 
Garside germ presentation \cite{Garide, Garsidebook}:
\begin{theorem}[\Cref{thm:conc}, \Cref{prop:nontrivial}]\label{thm:B}
  For every bipointed precubical set $K$ there is a functor $\Conc : \Chs(K) \to \BraidPos$ into
  the positive braid monoids, sending a morphism to the germ of the permutation by which it
  reorders events. It is natural in $K$, and it is nontrivial if $K$ contains a 2 cell.
\end{theorem}

\subsection{Formalisation}

Everything above except the topology is formalised in Lean~4 on top of mathlib \cite{Cubical}.
It also builds the foundational precubical theory, the neccesary results from \cite{PZ}.
Some formalisations are slightly modified from the text, but those deviations are noted.
It also provides a verified computation for $\Conc$.

\subsection{Notation}

To fix notation, let $\Cube$ be the box category: its objects are the cubes $\cube{n}$ for
$n \ge 0$, and its morphisms are the face embeddings (composites of the coface maps
$\delta_i^\epsilon$). A \emph{precubical set} is a presheaf on $\Cube$; write $\pSet$ for the
category of precubical sets. Concretely, a precubical set $K$ has, for each $n \ge 0$, a set $K_n$
of $n$-cells, together with face maps
\[
  d_i^\epsilon : K_n \to K_{n-1} \qquad (1 \le i \le n,\ \epsilon \in \{0,1\}),
\]
where $i$ names a direction and $\epsilon$ a side, subject to the precubical identities
$d_i^\epsilon d_j^\eta = d_{j-1}^\eta d_i^\epsilon$ for $i < j$. We write $\cube{n}$ both for the
standard $n$-cube as an object of $\Cube$ and for the presheaf it represents, and we set
$[n] := \{1, \dots, n\}$, so that $[0] = \emptyset$. Yoneda identifies the two descriptions of a
cell,
\[
  K_n \;=\; K(\cube n) \;=\; \Hom_{\pSet}(\cube{n}, K),
\]
and we pass between them without comment; in particular $K_0$ is the set of vertices.

We usually work with precubical sets carrying designated start and end vertices. A \emph{bipointed}
precubical set is a precubical set $K$ with two chosen vertices $\init_K, \fin_K \in K_0$,
and morphisms are required to preserve them; write $\BPSet$ for the resulting category. Each cube
$\cube{n}$ is bipointed by its initial vertex $(0,\dots,0)$ and final vertex $(1,\dots,1)$. We
write $Z$ for the final object of $\BPSet$.

\begin{convention}
Named ambient categories are set in bold ($\Cat$, $\Set$, $\Cube$, $\Braid$); constructions applied
to an object are set upright ($\Ch(K)$, $\Run(K)$, $\Sal(n)$). Functors are written with $\to$; we
reserve $\Rightarrow$ for natural transformations. The standard $n$-cube is $\cube{n}$;
the symbol $\square$ names the category of precubical sets.
\end{convention}

\begin{remark}[Variance]\label{rem:variance}
The $\op$ in \Cref{thm:A} is merely an artefact of choices made by different 
literature we reference. That is, in the cube chain literature 
\cite{PZ, Z2} a morphism of $\Ch(K)$ runs from a \emph{finer} chain to
a \emph{coarser}. And in the arrangement literature \cite{Salvetti, Paris} the face
order runs from \emph{smaller} faces to \emph{larger} ones. 
\end{remark}

\section{Background: cube chains}\label{sec:background}

We recall two constructions on $\BPSet$ from \cite{PZ, Z2}.

The first is the \textbf{wedge} $\vee$. Given $P, Q \in \BPSet$, the wedge $P \vee Q$ is the
pushout, formed in $\pSet$, gluing $P$'s final vertex to $Q$'s initial vertex:
\[
\begin{tikzcd}
  \cube{0} \arrow[r, "\init_Q"] \arrow[d, "\fin_P"'] & Q \arrow[d] \\
  P \arrow[r] & P \vee Q \arrow[ul, phantom, very near start, "\ulcorner"]
\end{tikzcd}
\]
Note that the two legs are maps of $\pSet$, not of $\BPSet$. The wedge $P \vee Q$ is bipointed by
$\init_P$ and $\fin_Q$, and $(\BPSet, \vee, \cube{0})$ is monoidal with unit the point $\cube{0}$.
The \textbf{serial wedge} of a list of \emph{positive} naturals is
\[
  \bigvee[a_1, \dots, a_r] := \cube{a_1} \vee \cdots \vee \cube{a_r},
\]
with $\bigvee[\,] = \cube{0}$. Write $\Wdg \subseteq \BPSet$ for the full subcategory of serial
wedges.

The second construction is $\Ch(K)$. For $K \in \BPSet$, let $\Ch(K)$ be the full subcategory of the slice $\BPSet / K$ spanned by the
serial wedges: an object is a bipointed map $\bigvee a \to K$, and a morphism
$(\bigvee a \to K) \to (\bigvee b \to K)$ is a bipointed map $\bigvee a \to \bigvee b$ commuting
over $K$. Such a morphism runs from a finer chain to a coarser one.

Finally, two properties of $K$ from \cite{PZ} recur. We say $K$ \emph{admits altitude} if there is
a function $\alt : K_0 \to \Z$ with $\alt(\fin(e)) = \alt(\init(e)) + 1$ for every edge
$e \in K_1$. Following \cite[\S2]{PZ} verbatim, $K$ is \emph{non-self-linked} (NSL) if every
$\square$-map $\cube{n} \to K$ is injective, for all $n \ge 0$. 

We use the following, all from \cite{PZ}.

\begin{lemma}[{\cite[\S2]{PZ}}]\label{lem:wedge-monoidal}
  The $\vee$ operator is a monoidal product on $\BPSet$, with unit $\cube 0$.
\end{lemma}

\begin{lemma}\label{lem:ch-lax}
  $\Ch : \BPSet \to \Cat$ is a lax monoidal functor from $(\BPSet, \vee)$ to $(\Cat, \times)$: the
  comparison $\Ch(P) \times \Ch(Q) \to \Ch(P \vee Q)$ concatenates chains.
\end{lemma}

\begin{lemma}\label{lem:ch-NSL}
  When $K$ admits altitudes and is NSL, the functor of \Cref{lem:ch-lax} is an isomorphisms of categories,
  since both categories are skeletal and posets. 
  This is a easy corrolary of {\cite[Lem.~2.11]{PZ}}
\end{lemma}


\begin{lemma}\label{lem:wedges-nice}
  Serial wedges admit altitude and are NSL. And specifically so the
  final vertex of $\bigvee a$ has altitude $\sum_i a_i$.
\end{lemma}

\begin{lemma}[{\cite[Prop.~2.7]{PZ}}]\label{lem:alt-preserved}
  Let $P, Q$ be connected bipointed precubical sets admitting altitude functions. Then every
  bipointed map $f : P \to Q$ preserves altitude: $\alt_Q \circ f = \alt_P$.
\end{lemma}

This is all the foundational material we need.

\section{Wedge machinery}\label{sec:wedge}
Most of the technical material in this paper involves bookkeeping around coordinates of wedges.
So we build a bit of machinery here to assist.
\begin{lemma}\label{lem:kan-left}
  A functor $P : \Cube \to \Set$ extends, by left Kan extension along the Yoneda embedding,
  to a cocontinuous $E(P) : \pSet \to \Set$ with $E(P)(\cube n) = P(\cube n)$. 
  If $P(\cube 0) = \emptyset$ then for every serial wedge
  \begin{equation}\label{eq:kan-left}
    E(P)\Bigl(\bigvee a\Bigr) \;\cong\; \coprod_i P(\cube{a_i}).
  \end{equation}
\end{lemma}
\begin{proof}
  Applying $E(P)$ to the pushout defining $A \vee B$ gives
  $E(P)(A \vee B) = E(P)(A) \sqcup_{P(\cube 0)} E(P)(B)$, which is the coproduct when
  $P(\cube 0) = \emptyset$.
\end{proof}

\begin{lemma}\label{lem:kan-right}
  Let $P$ be a precubical set and put $E(P) := \Hom_{\pSet}(-, P) : \pSet\op \to \Set$, the right
  Kan extension of $P$ along Yoneda. 
  If $P_0$ is a singleton then for every serial wedge
  \begin{equation}\label{eq:kan-right}
    E(P)\Bigl(\bigvee a\Bigr) \;\cong\; \prod_i P_{a_i}.
  \end{equation}
\end{lemma}
\begin{proof}
  A hom functor takes colimits in its first variable to limits, so $E(P)$ sends the pushout defining
  $A \vee B$ to the pullback $E(P)(A) \times_{E(P)(\cube 0)} E(P)(B)$. Here $E(P)(\cube 0) = P_0$ is
  a singleton, hence terminal in $\Set$, and a pullback over a terminal object is a product. 
\end{proof}

\begin{definition}\label{def:coord}
  Let $\Coord : \Cube \to \Set$ send the cube $\cube{n}$ to its set of directions $[n]$, and send a
  face embedding $g : \cube{n} \to \cube{m}$ to the monotone injection
  $g_* : [n] \hookrightarrow [m]$ that names the $n$ directions $g$ leaves free. Since
  $\Coord(\cube{0}) = [0] = \emptyset$, \Cref{lem:kan-left} applies:
  \[
    E(\Coord)\Bigl(\bigvee a\Bigr) \;\cong\; \coprod_i [a_i].
  \]
  For a morphism $f : \bigvee a \to \bigvee b$ we write $f_* := E(\Coord)(f)$.
\end{definition}

\begin{definition}\label{def:blk}
  Write $\blk(c)$ for the summand index of a coordinate $c \in \coprod_i [a_i]$. 
  By the pushout defining $\bigvee a$, a morphism $f : \bigvee a \to \bigvee b$ 
  is determined by the $a_i \to \bigvee b$.
  But each such image lies in a single $\cube{b_j}$ since $a_i > 0$. 
  So it corresponds to a order preserving face embedding
  \[
    f_i : \cube{a_i} \longrightarrow \cube{b_{\blk(f)(i)}}.
  \]
  This defines $\blk(f)$, and describes $f_*$ on coordinates:
  \begin{equation}\label{eq:blk-coord}
    f_*(i, d) \;=\; \bigl(\blk(f)(i),\ (f_i)_*(d)\bigr), \qquad d \in [a_i].
  \end{equation}
  In particular $\blk \circ f_* = \blk(f) \circ \blk$.
\end{definition}

\begin{lemma}\label{lem:coord-injective}
  For any morphism $f : \bigvee a \to \cube{m}$ in $\pSet$, the function $f_*$ is injective.
\end{lemma}
\begin{proof}
  Induct on $a$. The empty case is $f_* : [0] \to [m]$, trivially injective. 
  For the induction case, $f : (\bigvee a) \vee \cube{n} \to \cube{m}$, which by the pushout splits into
  $g : \bigvee a \to \cube{m}$ and $h : \cube{n} \to \cube{m}$ with $f_* = g_* \sqcup h_*$ by (\Cref{lem:kan-left}). 
  Here $g_*$ is injective by the induction hypothesis, and $h_*$ is the monotone injection for the face embedding $h$.

  So it suffices that $\im g_*$ and $\im h_*$ are disjoint. Let $c \in \{0,1\}^m$ be the image of
  the glued vertex $\fin(\bigvee a) = \init(\cube{n})$. Coordinates only increase along a map into
  a cube, so every direction $g$ leaves free is already $1$ at $c$, while every direction $h$
  leaves free is still $0$ at $c$.
\end{proof}

\begin{lemma}\label{lem:coord-bijective}
  For any morphism $f : \bigvee a \to \bigvee b$ in $\BPSet$, the function $f_*$ is bijective and $\blk(f)$ is monotone.
\end{lemma}
\begin{proof}
  To prove injective and monotone, we go by induction on the length of $b$, generalising over $a$.
  If $b$ is empty, then so is $a$ by \Cref{lem:alt-preserved} and \Cref{lem:wedges-nice}. 
  So both $\blk(f)$ and $f_*$ are the empty function.

  Now consider $f : \bigvee a \to \cube{m} \vee \bigvee b$. 
  By \Cref{lem:wedges-nice} and \Cref{lem:ch-NSL}, 
  $\Ch(\cube{m}) \times \Ch(\bigvee b) = \Ch(\cube{m} \vee \bigvee b)$. 
  We can view $f$ as an object of $\Ch(\cube{m} \vee \bigvee b)$. 
  So $a$ splits as $a = a_l \ast a_r$, and $f$ splits as
  \[
  (f_1 : \bigvee a_l \to \cube{m}, f_2 : \bigvee a_r \to \bigvee b)
  \]
  Since $f = f_1 \vee f_2$, by (\Cref{lem:kan-left}) we have
  \[
    f_* = (f_1 \vee f_2)_* = (f_1)_* \sqcup (f_2)_* 
  \]
  Now $(f_2)_*$ is injective by the induction hypothesis and $(f_1)_*$ is injective by \Cref{lem:coord-injective}.
  This is a disjoint union of injections with disjoint codomains, hence $f_*$ is injective.
  For $\blk$, note
  \[ 
  \blk(f) = \blk(f_1) \sqcup (1 + \blk(f_2))
  \]
  Now $\blk(f_1)$ is always $1$, and $1+\blk(f_2)$ is monotone by induction, and $>1$.
  So $\blk(f)$ is monotone as well.

  For surjectivity of $f_*$, we count. By \Cref{lem:wedges-nice} the final vertex of $\bigvee a$ has altitude
  $\sum_i a_i$, and likewise for $b$; by \Cref{lem:alt-preserved} the bipointed map $f$ preserves
  altitude so $\sum_i a_i = \sum_j b_j$. So $f_*$ is an injective function between finite sets with 
  equal cardinality. So $f_*$ is a bijection.
\end{proof}

\section{The braid arrangement and the Salvetti poset}\label{sec:arrangement}

The construction of $\Chs$ ports the following technique to precubical sets.\footnote{
  The lean repo uses Conditional Oriented Matriods instead of ordered set partitions to define the braid 
  arrangement, but proves they are equivalent
}
We describe the technique here, following Paris \cite{Paris}, and build $\Chs$ in the next section.

Let $\Face(n)$ be the set of total preorders on $[n]$. A total preorder $F$,
induces an equivalence relation $\sim_F$, which are linearly ordered by $<_F$.
The classes of $\sim_F$ are called blocks, which give an ordered set partition of $[n]$.
Order $\Face(n)$ by
\[
  F \leq G \quad:=\quad {<_F} \ \subseteq\ {<_G},
\]
This is the poset of \textbf{covectors}, or faces, of the braid arrangement 
$A_{n-1} = \{\,x_i = x_j \,\}_{i < j}$ in $\R^n$. A point $x$ gives
the covector $i \leq_F j := x_i \leq x_j$, the weak order of its coordinates. The maximal
covectors are those with all distinct points, that is the linear orders on $[n]$; these are the \textbf{topes},
and they correspond to the components of $\R^n \setminus \bigcup A_{n-1}$.

\begin{definition}\label{def:sal}
The \textbf{Salvetti poset} is
\[
  \Sal(n) := \bigl\{\, (F, C) \ \bigm|\ F \in \Face(n),\ C \text{ a tope},\ F \leq C \,\bigr\}.
\]
To defin e the order on $\Sal$, we first need to define composition. Let $F$ and $G$ be faces, and define $F \circ G$ by
\[
  i \leq_{F \circ G} j \iff i <_F j \text{ or } \big( i \sim_F j  \text{ and } i \leq_G j \big).
\]

In particular $F \leq F \circ G$, and $F \circ C$ is again a tope whenever $C$ is. 
We give $\Sal(n)$ a partial order structure
\[
  (F, C) \preceq (F', C') \quad:\!\iff\quad F \leq F' \ \text{ and }\ C' = F' \circ C.
\]
\end{definition}

This is the order of \cite{Paris}; see \cite{DDP} for the form used in the formalisation.

\begin{theorem}[Salvetti \cite{Salvetti}; see also \cite{BjornerZiegler, Paris}]\label{thm:salvetti}
  $|\Sal(n)| \simeq \Conf(n,\C) = \{z \in \C^n : z_i \neq z_j \text{ for } i \neq j\}$. 
  This is the ordered configuration space on $n$ points, as well as the complement of the complexified $A_{n-1}$.
  So $\pi_1|\Sal(n)| \cong P_n$, the pure braid group.
\end{theorem}

The packaging we will imitate is as a Grothendieck construction. Let
\[
  \Tope : \Face(n) \to \Set, \qquad \Tope(F) := \{ C \text{ a tope} : F \leq C \},
\]
be the covariant functor sending $F \leq F'$ to the map $C \mapsto F' \circ C$. Then
\[
  \Sal(n) \;\cong\; \textstyle\int \Tope,
\]
the Grothendieck construction: its objects are the pairs $(F, C)$ with $C \in \Tope(F)$, and its morphisms
are exactly the relations $\preceq$ above. Intuitively, each face is annotated with a compatible
tope, a local orientation; crossing a face can flip the orientation, and these flips are the braid
letters.

\section{The complexification \texorpdfstring{$\Chs$}{Ch*}}\label{sec:complexification}

The first observation that suggests a bridge is feasible here is that
\begin{lemma}\label{lem:ch-face}
  $\Ch(\cube n) \cong \Face(n)\op$
\end{lemma}
By \Cref{lem:coord-bijective} a chain of $\cube n$ is just an ordered set partition of its
directions, and a morphism of chains is a refinement. The rest is rote, and formalized in Lean.
But, to establish an analogy that works for any precubical set, we need 
to produce a precubical notion of tope. 
\subsection{Runs}

\begin{definition}
  A serial wedge is \textbf{one dimensional} if all its entries are $1$.
\end{definition}

\begin{definition}
  Let $\Run(K)$ be the set of one dimensional chains in $\Ch(K)$.
\end{definition}

\begin{lemma}\label{lem:run-monoidal}
  $\Run$ forms a monoidal functor from lists of positive integers to Set
\end{lemma}
\begin{proof}
  Note that wedge respects one dimensionality, so $\Run(K)$ is can also be seen 
  as a full, monoidal subcategory of $\Ch(K)$. 
  So $\Run$ is the composition of two monoidal functors. 
  One sending lists of positive integers to $\Wdg$, and monoidal inclusion inclusion $\Wdg \subset \BPSet$
\end{proof}

Now, we want a contravariant operation from $\Ch(K)$ to runs, 
mirroring the order from Salvetti:
\[
  (\bigvee a \to \bigvee b) \;\longmapsto\; (\Run(\bigvee b) \to \Run(\bigvee a)).
\]
We will define a functor $\rho:\Cube\op \to \Set$ which will give us this shape. 
First, note that every morphism $f : \cube n \to \cube m$ is a face embedding. 
So has a corresponding projection on cells $\hat{f} : \cube m \to \cube n$.
Notably, the projection is monotolically decreasing on cell dimension.
With that, we can define $\rho$ as follows

\begin{definition}\label{def:rho}
  On objects, let $\rho(\cube n) = \Run(\cube n)$. By the pushout defining the wedge, a run of
  $\cube m$ is a list $[c_1, \dots, c_m]$ of composable edges from $\init$ to $\fin$. For a face
  embedding $f : \cube n \to \cube m$, let
  \[
    \rho(f)[c_1, \dots, c_m] \;:=\; [\,\hat f(c_1), \dots, \hat f(c_m)\,]
    \quad\text{with the } 0\text{-cells deleted}.
  \]
  This is again a run: $\hat f(c_k)$ will preserve exactly $n$ coordinates, giving $n$ cells.
  The projection preseves boundary maps, so even after deleting 0 cells, it remains a wedge map.
  And $\hat f$ carries $\init$ to $\init$ and $\fin$ to $\fin$.\footnote{
    The lean defines $\rho$ via a slight detour with cube chains instead of wedge maps 
    to avoid some issues with ``Fin''. But the approaches are proven equivalent}

  In coordinates, $r_*(k)$ is the free direction of $c_k$, so the surviving steps are those in
  $S := r_*^{-1}(\im f_*)$, and they keep their relative order. Writing $\iota : [n] \to S$ for the
  increasing bijection,
  \begin{equation}\label{eq:rho-restrict}
    \rho(f)(r)_* \;=\; f_*^{-1} \circ r_* \circ \iota .
  \end{equation}
\end{definition}

\begin{lemma}\label{lem:rho-functor}
  $\rho : \Cube\op \to \Set$ is a functor and $\rho(\cube{0})$ is a singleton; so $\rho$ is a
  precubical set with a single vertex.
\end{lemma}
\begin{proof}
  The projection of the identity embedding is the identity.
  And projections compose to projections, so $\rho$ is a functor.
  The empty wedge map is the sole member of $\rho(\cube 0)$.
\end{proof}

So $\rho$ is a precubical set, and we write $E(\rho) := \Hom_{\pSet}(-, \rho) : \BPSet\op \to \Set$.

\begin{lemma}[$\rho$ classifies runs]\label{lem:rho-classifies}
  For every serial wedge $\bigvee a$ there are bijections
  \[
    E(\rho)\bigl(\textstyle\bigvee a\bigr) \;=\; \Hom_{\pSet}\bigl(\textstyle\bigvee a,\ \rho\bigr)
      \;\cong\; \prod_i \Run(\cube{a_i}) \;\cong\; \Run\bigl(\textstyle\bigvee a\bigr).
  \]
\end{lemma}
\begin{proof}
  The left congruence is \Cref{lem:kan-right}. The right is the monoidality of $\Run$. 
\end{proof}

We will freely identify $E(\rho)(\bigvee a)$ with $\Run(\bigvee a)$.

\subsection{The construction}

\begin{definition}\label{def:chstar}
Let $W(K) : \Ch(K) \to \Wdg$ be the forgetful functor $(\bigvee a \to K) \mapsto \bigvee a$, natural
in $K$: for $u : K \to L$ we have $W(L) \circ \Ch(u) = W(K)$. Set
\[
  \Lines(K) := E(\rho) \circ W(K)\op,
\]
a presheaf on $\Ch(K)$, and
\[
  \Chs(K) := \textstyle\int \Lines(K),
\]
the category of elements of $\Lines(K)$, with projection $\pi : \Chs(K) \to \Ch(K)$.
\end{definition}

We will see that defining things abstractly can some conveinences later.  
But first let us unpack the definition of $\Chs$ to have a more concrete presentation of it.
Objects of $\Chs(K)$ are pairs
\[
  (\bigvee a \to K,\ x \in \Run(\bigvee a)), \text{ i.e. } \bigvee[1,\dots,1] \to \bigvee a \to K,
\]
a cube chain together with a linear ordering of its events.
The intuition here matches the Salvetti story precisely: we have a wedge map into $K$, coupled with
compatible orientation information. Morphisms are a little uglier. A morphism
\[
  (\bigvee a \to K,\ x \in \Run(\bigvee a)) \longrightarrow (\bigvee b \to K,\ y \in \Run(\bigvee b))
\]
unpacks to
\begin{enumerate}
  \item A morphism $f : (\bigvee a \to K) \to (\bigvee b \to K)$,
  i.e.\ a bipointed map $f' : \bigvee a \to \bigvee b$ commuting over $K$.
  \item The condition $x = \Lines(f)(y)$:
  we take the run $y$, project it backwards along $\hat{f}$, and require the result to be $x$.
\end{enumerate}

The diagram below records the story. The solid arrows are morphisms of $\Chs(K)$; the dashed
lines are not. $E(\rho)(f)$ is the projection associated with $f$. And $\sigma(f)$ is 
an induced permutation we will study in the next section
\[
\begin{tikzcd}[row sep=huge, column sep=huge]
  \bigvee[1,\dots,1] \arrow[r, "x"] \arrow[d, perm, "\sigma(f)"']
    & \bigvee a \arrow[d, "f"] \arrow[rd] & \\
  \bigvee[1,\dots,1] \arrow[r, "y"']
    & \bigvee b \arrow[u, runact, bend left=85, "E(\rho)(f)"] \arrow[r] & K
\end{tikzcd}
\]

\subsection{The crossing permutation}

Let's look more closely at how $\sigma$ is constructed.
Let $N = \Sigma_i a_i = \Sigma_j b_j$. Then define  
\[
  \sigma(f) := y_*^{-1} \circ f'_* \circ x_* \,
\]
and observe that by \Cref{lem:coord-bijective}, $\sigma(f)$ is a composition of bijections, 
so $\sigma(f) \in \Sigma_n$.
Also, the form $y_* \circ \sigma(f) = f'_* \circ x_*$ will be convenient.
This permutation $\sigma(f)$ is what will power our braid functor.

\begin{lemma}\label{lem:sigma-functor}
$\sigma : \Chs(K) \to \bigsqcup_N \Sigma_N$ is a functor, where the target is the groupoid with
objects the naturals and $\Hom(N,N) = \Sigma_N$.
\end{lemma}
\begin{proof}
Along a composite, the middle map $y_*$ cancels,
\[
  \sigma(g \circ f) = z_*^{-1} \circ g'_* \circ f'_* \circ x_*
    = \bigl(z_*^{-1} \circ g'_* \circ y_*\bigr)\bigl(y_*^{-1} \circ f'_* \circ x_*\bigr)
    = \sigma(g)\,\sigma(f),
\] 
and
\[
  \sigma(\id) = x_*^{-1} \circ (\id)_* \circ x_* = \id. \qedhere
\]
\end{proof}

\begin{lemma}\label{lem:within-summand}
  Let $f : (\bigvee a, x) \to (\bigvee b, y)$ in $\Chs(K)$ and suppose
  $\blk(x_*(k)) = \blk(x_*(l))$. Then $k < l$ if and only if $\sigma(f)(k) < \sigma(f)(l)$.
\end{lemma} 
\begin{proof}
  Let $i = \blk(x_*(k))$. Then $x \in Run(\bigvee a) \simeq \prod_j \Run(\cube {a_j})$, by \Cref{lem:run-monoidal}.
  So let $x_i$ be its $i^{\text{th}}$ component.
  Likewise, let $j = \blk(f')(i)$, and $y_j$ be its $j^{\text{th}}$ component.

  Now, since $x$ is one dimensional, $\blk \circ x_* = \blk(x)$, which is monotone (\Cref{lem:coord-bijective}). 
  So $x_*^{-1}(i)$ is an interval, order-isomorphic to the positions
  $[a_i]$ of $x_i$, and likewise for $y$ and $[b_j]$.

  The morphism condition restricts to $x_i = \rho(f_i)(y_j)$ (\Cref{def:blk}), 
  so $\sigma(f) = y_*^{-1} \circ f'_* \circ x_*$ restricts
  to $(y_j)_*^{-1} \circ (f_i)_* \circ (x_i)_*$, which is the increasing
  inclusion $[a_i] \to [b_j]$. Both identifications preserve order, so $k < l$ if and only if
  $\sigma(f)(k) < \sigma(f)(l)$.
\end{proof}

\subsection{Comparison with the Salvetti poset}

\begin{theorem}\label{thm:sal}
  For every $n \ge 0$ there is an isomorphism of posets $\Chs(\cube n) \cong \Sal(n)\op$.
  Consequently $|\Chs(\cube n)| \simeq \Conf(n,\C)$ and $\pi_1|\Chs(\cube n)| \cong P_n$.
\end{theorem}
The proof is straightforward, although a bit tedius. 
Start with \Cref{lem:ch-face}. Then send $\Run(\cube n)$ to topes.
For morphisms, the composition operator agrees with the projection. This is machine-checked in \cite{Cubical}.

\section{The braiding}\label{sec:braiding}

You'll notice that the $K$ side is not involved in the computation of the permutation,
or the proofs of its functoriality. We can formalize why this happens nicely.

\begin{lemma}\label{lem:chs-Z}
  Let $Z$ be the terminal precubical set. 
  Then $\Ch(Z) \cong \Wdg$ and  $\Chs(Z) \cong \Wdg/\rho$, the comma category of serial
  wedges over $\rho$.
\end{lemma}
\begin{proof}
  Since $Z$ is terminal, each serial wedge has exactly one bipointed map to it and every map of
  wedges commutes over it; so $W(Z) : \Ch(Z) \to \Wdg$ is an isomorphism. Hence
  $\Lines(Z) = E(\rho) \circ W(Z)\op = E(\rho)|_{\Wdg}$. Taking categories of elements,
  $\Chs(Z) = \int \Lines(Z) = \int E(\rho)|_{\Wdg} = \Wdg/\rho$, the last step because the category
  of elements of $\Hom_{\pSet}(-,\rho)$ is the comma category.
\end{proof}

\begin{lemma}\label{lem:pullback}
  $\Chs$ is a functor $\BPSet \to \Cat$, natural in $K$ in the sense that
  $\pi : \Chs \Rightarrow \Ch$ is a natural transformation. Moreover, for every $K$ the naturality
  square at the unique map $!_K : K \to Z$ to the final object,
  \[
  \begin{tikzcd}[column sep=large, row sep=large]
    \Chs(K) \arrow[r, "\Chs(!_K)"] \arrow[d, "\pi_K"']
      \arrow[dr, phantom, very near start, "\lrcorner"]
      & \Chs(Z) \arrow[d, "\pi_Z"] \\
    \Ch(K) \arrow[r, "\Ch(!_K)"'] & \Ch(Z)
  \end{tikzcd}
  \]
  is a strict pullback in $\Cat$.
\end{lemma}
\begin{proof}
  For $u : K \to L$, naturality of $W$ gives
  $\Lines(L) \circ \Ch(u)\op = E(\rho) \circ \bigl(W(L) \circ \Ch(u)\bigr)\op = \Lines(K)$.
  So $\Chs(u)(c, x) := (\Ch(u)(c),\ x)$, and acts on morphisms via $\Ch(u)$.
  So we get functoriality and $\pi_L \circ \Chs(u) = \Ch(u) \circ \pi_K$.

  Now take $u = !_K$, so $\Lines(K)$ is the restriction of $\Lines(Z)$ along $\Ch(!_K)$.
  Restricting a presheaf pulls back its category of elements, strictly: both $\Chs(K)$ and
  $\Ch(K) \times_{\Ch(Z)} \Chs(Z)$ have as objects the pairs
  $(c \in \Ch(K),\ x \in \Lines(Z)(\Ch(!_K)c))$, with the same morphisms.
\end{proof}

Note that $K$ does not appear in the right-hand column: $\Chs(K)$ is determined by $\Ch(K)$
together with the single universal case $\Chs(Z)$.

\subsection{Braid words from permutations}

Now, a morphism $f$ of $\Chs(K)$ has a permutation $\sigma(f)$, and it does not depend on $K$ 
in the sense above.
So it suffices to consider morphisms $f$ of $\Chs(Z)$. 
But we still need a device for turning permutations into braid words. 
Which brings us to the Garside germ presentation of braids \cite{Garside, Garsidebook}. 
For any $n \in \N$, and $\sigma \in \Sigma_n$ write
\[
  \Inv(\sigma) := \{\, (i,j) \in [n] \times [n] \mid i < j \text{ and } \sigma(j) < \sigma(i) \,\}
\]
for its set of \emph{inversions}, and $\ell(\sigma) := |\Inv(\sigma)|$ for its \emph{length}. 
\begin{definition}\label{def:braid}
  Let $\BraidPos$ be the category with objects the naturals and $\Hom(N,N) = B_N^+$ the positive
  braid monoid on $N$ strands in its \emph{germ presentation}: one generator $[\sigma]$ for each
  $\sigma \in \Sigma_N$, subject to
  \[
    [\sigma][\sigma'] = [\sigma\sigma'] \qquad\text{whenever}\qquad \ell(\sigma\sigma') = \ell(\sigma) + \ell(\sigma').
  \]
  Write $\Braid$ for the corresponding category of braid \emph{groups}, $\Hom(N,N) = B_N$; the
  canonical functor $\BraidPos \to \Braid$ is faithful by Garside's theorem \cite{Garside}.
\end{definition}

The key fact is a ``no double crossings'' feature of $\Chs$: along a composite, no two events cross
each other twice. Let's first state this in pure combinatorics.

\begin{lemma}\label{lem:no-double-crossing}
  Let $f$ and $g$ be permutations of $[N]$,
  and suppose that $\Inv(f) \subseteq \Inv(g \circ f)$.
  Then $\ell(g \circ f) = \ell(g) + \ell(f)$.
\end{lemma}
\begin{proof}
Every element of $\Inv(g\circ f)$ either lies in $\Inv(f)$ or not, so
\[
  \Inv(g\circ f) \;=\; \big(\Inv(g\circ f)\cap \Inv(f)\big)\ \sqcup\ \big(\Inv(g\circ f)\setminus \Inv(f)\big).
\]
By hypothesis $\Inv(f)\subseteq \Inv(g\circ f)$, so the first set equals $\Inv(f)$ and contributes $\ell(f)$.

For the second, a pair $k < l$ and $(k,l)\notin \Inv(f)$ has $f(k)<f(l)$, and then $(k,l)\in \Inv(g\circ f)$
iff $g(f(k))>g(f(l))$, i.e. iff $(f(k),f(l))\in \Inv(g)$. Since $f$ is a bijection,
$(p,q)\mapsto(f(p),f(q))$ restricts to a bijection
\[
  \Inv(g\circ f)\setminus \Inv(f)\ \xrightarrow{\ \sim\ }\ \{(p,q)\in \Inv(g)\;:\;f^{-1}(p)<f^{-1}(q)\}.
\]
This target is all of $\Inv(g)$: otherwise take $(p,q)\in \Inv(g)$ and set
$k=f^{-1}(q)<l=f^{-1}(p)$. Then $f(k)=q>p=f(l)$ gives $(k,l)\in \Inv(f)$, while
$g(f(k))=g(q)<g(p)=g(f(l))$ gives $(k,l)\notin \Inv(g\circ f)$, contradicting $\Inv(f)\subseteq \Inv(g\circ f)$.
Hence the second set contributes $\ell(g)$, and $\ell(g\circ f)=\ell(f)+\ell(g)$.
\end{proof}

\begin{lemma}[crossings are intra-block]\label{lem:inv-restrict}
Let $g : (\bigvee c, r) \to (\bigvee b, q)$ in $\Chs(Z)$ and $(k,l) \in \Inv(\sigma(g))$. Then
$(\blk \circ q_* \circ \sigma(g))(k) = (\blk \circ q_* \circ \sigma(g))(l)$.
\end{lemma}
\begin{proof}
Put $u := \sigma(g)(k)$ and $v := \sigma(g)(l)$, and $g' : \bigvee c \to \bigvee b$ be the induced wedge map. 
So $k < l$ and $v < u$. Since $q_* \circ \sigma(g) = g'_* \circ r_*$ we have $q_*(u) = g'_*(r_*(k))$ and $q_*(v) = g'_*(r_*(l))$,
so by \Cref{def:blk} and the monotonicity of $\blk(g')$ and of $\blk \circ r_* = \blk(r)$
(\Cref{lem:coord-bijective}),
\[
  \blk(q_*(u)) = \blk(g')\bigl(\blk(r_*(k))\bigr) \;\le\; \blk(g')\bigl(\blk(r_*(l))\bigr) = \blk(q_*(v)),
\]
using $k < l$. On the other hand $v < u$ and monotonicity of $\blk \circ q_* = \blk(q)$
(\Cref{lem:coord-bijective}) give
$\blk(q_*(v)) \le \blk(q_*(u))$. Hence $\blk(q_*(u)) = \blk(q_*(v))$.
\end{proof}

\begin{theorem}\label{thm:conc}
The map $f \mapsto [\sigma(f)]$ yields a functor $\Conc : \Chs(K) \to \BraidPos$, natural in $K$.
\end{theorem}
\begin{proof}
An object goes to the braid object on its number of coordinates, well defined by
\Cref{lem:coord-bijective}, and $\sigma(\id) = \id$. For
composition we need $[\sigma(f)][\sigma(g)] = [\sigma(f) \circ \sigma(g)]$, which by the germ
presentation holds once $\ell(\sigma(f) \circ \sigma(g)) = \ell(\sigma(f)) + \ell(\sigma(g))$, and by
\Cref{lem:no-double-crossing} it is enough to prove
$\Inv(\sigma(g)) \subseteq \Inv(\sigma(f) \circ \sigma(g))$
for composable $g : (\bigvee c, r) \to (\bigvee b, q)$ and $f : (\bigvee b, q) \to (\bigvee a, p)$.

Let $(k,l) \in \Inv(\sigma(g))$ and put $u := \sigma(g)(k)$, $v := \sigma(g)(l)$, so $v < u$. By
\Cref{lem:inv-restrict} the $(\blk \circ q_*)(u) = (\blk \circ q_*)(v)$, so
\Cref{lem:within-summand} applied to $f$ turns $v < u$ into $\sigma(f)(v) < \sigma(f)(u)$, which is
exactly $(k,l) \in \Inv(\sigma(f \circ g)) = \Inv(\sigma(f) \circ \sigma(g))$.

For naturality in $K$, note that $\sigma$ factors through $\Chs(Z)$, so 
$\Conc(K) = \Conc(Z) \circ \Chs(!_K)$ by \Cref{lem:pullback}.
\end{proof}

\begin{proposition}\label{prop:nontrivial}
For any precubical set $K$, if there is a 2 cell, then
$\Conc(K)$ is nontrivial.
\end{proposition}
\begin{proof}
  First, note that if $K$ has a 2-cell, then there is a map $u : \cube 2 \to K$.
  Then, by naturality 
  \[
  \begin{tikzcd}[row sep=large, column sep=large]
      \Chs(\cube 2)\arrow[d, "\Chs(u)"] \arrow[rd, "\Conc(\cube 2)"] & \\
      \Chs(K) \arrow[r, "\Conc(K)"'] & \BraidPos
  \end{tikzcd}
  \]
  Consider the map $p : \bigvee[1,1] \to \bigvee[2]$ going along the bottom edge and then the right
  edge, and the map $q : \bigvee[1,1] \to \bigvee[2]$ going along the left edge and then the top
  edge. We have
  \[
  \begin{tikzcd}[row sep=large, column sep=large]
    \bigvee[1,1] \arrow[r, "\id"] \arrow[d, perm, "\sigma(p)"']
      & \bigvee[1,1] \arrow[d, "p"] \arrow[rd] & \\
    \bigvee[1,1] \arrow[r, "q"']
      & \bigvee[2] \arrow[r, "f"'] & \cube 2
  \end{tikzcd}
  \]
  This is a morphism of $\Chs(\cube 2)$ from $(\bigvee[1,1] \xrightarrow{f \circ p} \cube 2,\ \id)$ to
  $(\bigvee[2] \xrightarrow{f} \cube 2,\ q)$: the condition $x = \Lines(p)(y)$ is automatic because
  $\Run(\bigvee[1,1])$ is a singleton. Its permutation is
  \[ 
    \sigma(p) = q_*^{-1} \circ p_* \circ \id = \{1 \mapsto 2,\ 2 \mapsto 1\}
  \], because $p$ traverses direction $1$ then direction $2$ while $q$ traverses direction $2$ then direction $1$. 
  This has an inversion, so $[\sigma(p)]$ is not the identity braid.
\end{proof}

\section{Next steps}\label{sec:next}

\begin{enumerate}
  \item\label{item:geometric} \textbf{What does $|\Chs(K)|$ compute?} It appears that $\Conc$ is 
        a measurement of the concurrency structure of $K$, but it's unclear exactly 
        what it computes in general. For euclidean complexes, it seems to have a slightly 
        some slightly less vague semantics, since every strand corresponds to a axis. 
        While $\Chs(K)$ as much information as $\Ch(K)$, $\Conc$ seems to depend only on the 3-skeleton.

  \item\label{item:grading} \textbf{A chain complex.} The category $\Chs(K)$ is graded by the shape
        of the wedges: (the sum of the dimensions) $-$ (the length of the list). Each morphism
        should decompose into a sequence of degree $1$ morphisms. More work is required to 
        see what kinds of properties this has.

  \item\label{item:sensitivity} \textbf{Sensitivity} Using the verified computations in Lean, 
      we have established several features. Firstly, the image of $\Conc$ seems insenstive
      to the topology. Both $\cube 3$ and the boundary of $\cube 3$ (that is, $\cube 3$ without it's top 3-cell)
      produce the whole of $P_n$. 
      However the kernels of $\Conc$ are quite distinct, so $\Conc$ itself is senstive to the topology.
      The degree 1 wedges determine which generators occur, and the degree 2 wedges determine which 
      artin relations are faithful. 

  \item\label{item:subdivision} \textbf{Braid Closures} 
      When looking at loops in the free groupoid of $\Chs(K)$, it make intuitive sense 
      to consider not just the braid, but the braid closure.
      This gives us a device for adding new strands to the braids, via the markov move.
      There is also a desire to build something invariant under subdivision, which 
      also adds new strands. Perhaps these goals align?
      Futhermore, it may give a nice braid theoretic explanation of why only the 3-skeleton occurs,
      as knots are all trivial in codimension 3 and above.

\end{enumerate}

\printbibliography

\end{document}
